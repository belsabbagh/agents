# Comments

- **Clarity over cleverness** — comments explain *why*, only where surrounding code doesn't already make it obvious.
- **No decorative comments** — no block comments before every field in a struct/record/type, no docstrings on trivial accessors, no `// variable name` on a line that already names the variable. Save doc comments for public API surfaces that need explanation.
